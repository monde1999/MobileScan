# MobileScan

## Important Functions:
1. MainActivity.save_rgb() --> (effective but the image is rotated)
2. MainActivity.save_depth() --> (buggy)
3. MainAcitivty.sendData() --> send rgb and depth files to the server
